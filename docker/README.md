# docker/

Docker Compose stack and Dockerfiles for the full Yapilayer platform.

`docker-compose.yml` arrives in Milestone 3 (postgres + mock-bank-simulator + platform) and is hardened to the complete single-command stack in Milestone 8. See ROADMAP.md.
