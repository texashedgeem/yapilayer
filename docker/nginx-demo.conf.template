server {
    listen ${NGINX_PORT};

    location /api/ {
        proxy_pass http://platform:8080;
        proxy_set_header Host $host;
    }

    location / {
        root /usr/share/nginx/html;
        # SPA fallback: client-side routes like /connected and /paid
        try_files $uri /index.html;
    }
}
