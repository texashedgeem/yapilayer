name: security

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "30 5 * * 1" # weekly, Monday 05:30 UTC

jobs:
  codeql:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read
    strategy:
      matrix:
        # javascript-typescript is added back in Milestone 6 when the first
        # real TS source lands — CodeQL fails on a repo with only manifests.
        language: [java-kotlin]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        if: matrix.language == 'java-kotlin'
        with:
          distribution: temurin
          java-version: "21"
      - uses: gradle/actions/setup-gradle@v4
        if: matrix.language == 'java-kotlin'
      - uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          build-mode: manual
      - name: Build for CodeQL (traced)
        if: matrix.language == 'java-kotlin'
        # --no-daemon so the CodeQL tracer sees the compiler; caches disabled so
        # compilation actually re-runs and gets traced
        run: ./gradlew --no-daemon --no-build-cache --no-configuration-cache clean compileJava
      - uses: github/codeql-action/analyze@v3

  dependency-review:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: high
