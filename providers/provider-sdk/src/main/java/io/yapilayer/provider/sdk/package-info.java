/**
 * The provider plugin contract — BankConnector, AIS/PIS provider ports and the provider registry.
 * Every bank connector implements these interfaces; core platform code depends only on this module,
 * never on individual connectors.
 */
package io.yapilayer.provider.sdk;
